# Local Development Database Configuration
# ============================================
# This profile is used when running with: 
#   ./mvnw spring-boot:run -Dspring-boot.run.profiles=local
# OR
#   export SPRING_PROFILES_ACTIVE=local
#   ./mvnw spring-boot:run

# Local PostgreSQL Database - UPDATE THESE VALUES FOR YOUR LOCAL SETUP!
spring.datasource.url=jdbc:postgresql://localhost:5432/coffee_tracker
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.datasource.driver-class-name=org.postgresql.Driver

# ============================================
# How to set up a local PostgreSQL database:
# ============================================
# 1. Install PostgreSQL (if not already installed)
# 2. Create the database:
#    CREATE DATABASE coffee_tracker;
# 3. Update the username and password above if different
